name: LID_validation_env
channels:
  - conda-forge
  - defaults
dependencies:
  - festim=1.4
  - matplotlib=3.8.3
  - numpy<1.25
  - sympy
  - nbconvert
  - ipykernel  # needed to run jupyter notebooks
  - pandas
  - pip:
    - h-transport-materials==0.17
    - pypalettes
    - matplotx